generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model settings {
  id   String  @id @db.VarChar(255)
  data String @default("{}")

  @@map("settings")
}
